	// Log::debug("\033[0;32mRequestParsing Reading");
    // struct timeval  tv;
    // struct tm       *timeinfo;
	// char			date[101];
	
    
    	// if (tmp == -1 && ret == 0)
		// {
		// 	std::cerr << "retry after" << std::endl;
			
		// 	if (gettimeofday(&tv, NULL) == 0)
		// 	{
		// 		timeinfo = localtime(&(tv.tv_sec));
		// 		strftime(date, 100, "%a %d %b 20%y %OH:%OM:%OS GMT", timeinfo);
		// 	}
			
		// 	response.assign("HTTP/1.1 503 Service Unavailable\r\nServer: Server/2.0\r\n")
		// 	.append("Date: ").append(date).append("\r\nRetry-After: 1\r\n\r\n");
		// 	std::cerr << response << std::endl;
		// 	write(fd, response.c_str(), response.size());
		// 	// close(fd);
		// 	std::cerr << "close" << std::endl;
		// 	return NULL;
		// }